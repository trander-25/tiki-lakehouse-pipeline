name: 'dbt_tiki'
version: '1.0.0'

profile: 'dbt_tiki'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_tiki:
    staging:
      +materialized: view
    marts:
      +materialized: external
      +format: parquet
